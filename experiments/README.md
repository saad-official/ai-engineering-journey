# Experiments

Spikes that compare approaches (A vs B) and end in a decision. Each folder has a `README.md` with: question, setup, measurements table, conclusion, decision recorded where (link to `TECHNOLOGY_STACK.md` or a project decision record).

Rows summarising each experiment go in `../EXPERIMENTS.md`.
